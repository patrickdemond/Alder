CMAKE_MINIMUM_REQUIRED( VERSION 2.6 )
PROJECT( AlderTesting )

SET( OPAL_HTTP_SOURCE
  OpalHttp.cxx
)

# Targets
ADD_EXECUTABLE( OpalHttp ${OPAL_HTTP_SOURCE} )

TARGET_LINK_LIBRARIES( OpalHttp
  ${CURL_LIBRARY}
  ${CRYPTO++_LIBRARIES}
)
INSTALL( TARGETS OpalHttp RUNTIME DESTINATION bin )
