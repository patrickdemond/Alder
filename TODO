A list of things which need to be done (in no particular order)
---------------------------------------------------------------

- add const expressions where possible (including const_iterator)
- consider adding a modal-less text display window to monitor and scroll the log 
- consider changing Exam.Downloaded from a t/f to an enum status: {EMPTY, PARTIAL, COMPLETE}
